/*Steg 1: Skapa Todo-interfacet*/

interface Todo {
    task: string,
    completed: boolean,
    priority: number
};

/*Steg 2: Implementera Todo-klassen */

class ToDoList {
    //Array som lagrar todo-objekt
    todos: Todo[];

    //Metod för att lägga till todos med prioritet

    //Metod för att markera todos som klara

    //Metod för att hämta listan av Todos

    //Metod för att spara todos till LocalStorage

    //Metod för att hämta todos från LocalStorage

}